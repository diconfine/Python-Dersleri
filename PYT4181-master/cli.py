# cd => current directory
# cd <klasör adı>   => sizi o dizine götürür. örnek " cd Desktop "
# mkdir (windows/mac) klasör oluşturur. örnek " mkdir YPT4181 "
# md    (windows)     klasör oluşturur. örnek " md PYT4181 "
# cd .. bir üst dizine çıkar (geri)
# (windows) " echo cli.py "     => bulunduğu dizin içerisinde cli.py adında bir belge oluşturur.
# (mac) " touch cli.py "      => bulunduğu dizin içerisinde cli.py adında bir belge oluşturur 
# dir (windows) - ls (macos) => bulunduğunuz dizini listeler.
# rm belgeadi    => adını verdiğiniz belgeyi siler
# rmdir dosyaAdi => adını verdiğiniz dosyayı siler

# tekli yorum satırı (#) bu alana yazılan kodlar derlenmez.


# git init  => git konfigurasyon dosyası ekler
# git add <*> veya <dosya adı>
# git commit -m "açıklama yazınız yaptığınız işle alakalı"
# git push -u origin <branch adı> => default branch master'dır
# git pull -u origin <branch adı> => github üzerinde değişiklik var ise, sizin projenize değişiklikleri alır.
